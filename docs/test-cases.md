# URL Shortener Test Cases

Test valid URLs, invalid URLs, repeated submissions, empty input, and generated-link copy behavior. Validation errors should not replace a previously successful result unexpectedly.